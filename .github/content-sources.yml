# Content Watch — source list
#
# Read by the "Content watch" workflow (.github/workflows/content-watch.md), which runs daily and reports new items from every source with `active: true`.
#
# HOW TO ADD A SOURCE
#   1. Append an entry below with a unique `id`, the feed/page `url`, and `active: true`.
#   2. If the source's domain isn't already covered by `network.allowed` in content-watch.md, add it there (see the comment next to that field).
#   3. Recompile so the lock file matches the new network allowlist: gh aw compile content-watch --strict
#   No code change is needed for the source list itself — only the workflow frontmatter needs a recompile, and only when a genuinely new domain is involved.
#
# HOW TO DEACTIVATE A SOURCE
#   Set `active: false`. Keep the entry — don't delete it — so there's a record of what used to be watched and why it stopped. Use `notes` to say why (site died, too noisy, replaced by another source, etc). No recompile needed to deactivate.
#
# FIELDS
#   id     - stable, unique, kebab-case. Used as the dedup-cache filename; don't reuse an id for a different source, and don't rename it once it has run at least once (renaming resets its "seen" history and everything currently in the feed will be reported as new).
#   name   - display name used in the digest.
#   url    - the feed or page URL to check.
#   type   - "rss" : an RSS/Atom feed URL. Preferred — stable, structured, cheap to diff. "page" : a plain web page with no feed. Best-effort scrape of the rendered page; less reliable. Use only when the source has no feed. "github-commits" : a GitHub commits Atom feed scoped to one path in a docs repo (https://github.com/<owner>/<repo>/commits/<branch>/<path>.atom). The workflow fetches each new commit's .diff and only reports commits whose diff changes substantive prose, not typo/metadata/freshness-bot commits — see content-watch.md.
#   active - true to include it in the daily run, false to skip it.
#   notes  - free text: why it's paused, quirks, anything worth remembering later.

sources:
  - id: simon-willison
    name: Simon Willison's Weblog
    url: https://simonwillison.net/atom/everything/
    type: rss
    active: true
    notes: ""

  - id: lesswrong
    name: LessWrong
    url: https://www.lesswrong.com/feed.xml
    type: rss
    active: true
    notes: ""

  - id: martin-fowler
    name: Martin Fowler
    url: https://martinfowler.com/feed.atom
    type: rss
    active: true
    notes: ""

  - id: fabric-updates-blog
    name: Microsoft Fabric Updates Blog
    url: https://community.fabric.microsoft.com/oxcrx34285/rss/board?board.id=fbc_fabricupdatesblogs
    type: rss
    active: true
    notes: ""

  - id: powerbi-updates-blog
    name: Power BI Updates Blog
    url: https://community.fabric.microsoft.com/oxcrx34285/rss/board?board.id=fbc_pbiupdatesblog
    type: rss
    active: true
    notes: ""

  - id: fabric-docs-onelake
    name: Fabric docs — OneLake
    url: https://github.com/MicrosoftDocs/fabric-docs/commits/main/docs/onelake.atom
    type: github-commits
    active: true
    notes: "Scoped to docs/onelake — ties to the shortcut/OneLake chapters."

  - id: fabric-docs-data-warehouse
    name: Fabric docs — Data Warehouse
    url: https://github.com/MicrosoftDocs/fabric-docs/commits/main/docs/data-warehouse.atom
    type: github-commits
    active: true
    notes: "Scoped to docs/data-warehouse — ties to the V-Order / Direct Lake chapters."

  - id: fabric-docs-enterprise
    name: Fabric docs — Enterprise (capacity & admin)
    url: https://github.com/MicrosoftDocs/fabric-docs/commits/main/docs/enterprise.atom
    type: github-commits
    active: true
    notes: "Scoped to docs/enterprise — ties to the capacity-unit chapter."

  - id: powerbi-docs-guidance
    name: Power BI docs — Guidance
    url: https://github.com/MicrosoftDocs/powerbi-docs/commits/main/powerbi-docs/guidance.atom
    type: github-commits
    active: true
    notes: ""

  - id: architecture-center-patterns
    name: Azure Architecture Center — Patterns
    url: https://github.com/MicrosoftDocs/architecture-center/commits/main/docs/patterns.atom
    type: github-commits
    active: true
    notes: "Direct match for the book's pattern-kind concepts (e.g. impedance-mismatch)."

  - id: azure-docs
    name: Azure docs (all services)
    url: https://github.com/MicrosoftDocs/azure-docs/commits/main.atom
    type: github-commits
    active: true
    notes: "Unscoped — whole repo, capped to the 20 most recent commits per run like every other source. Broad Azure/technology signal rather than a chapter-specific bridge; revisit if it's too noisy or too thin once the digest has run a few times."

  - id: example-page
    name: "EXAMPLE — replace before activating"
    url: https://example.com/updates
    type: page
    active: false
    notes: "Placeholder showing the \"page\" shape. type: page has no feed to diff against, so it's the least reliable option and depends on the page having a recognizable list of dated items — prefer an RSS/Atom feed when the source offers one."
